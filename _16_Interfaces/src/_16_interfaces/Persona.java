
package _16_interfaces;
public interface Persona { //Interface principal
    
    // En una interface casi nunca se usan atributos
    
    // Metodos
    public void hablar(); // En una interface no es necesario colocarle abstract, en una interface se sobreentiende que se tiene metodos asbtractos
    
}
